# ASR Backend - 本地版本
# 包含完整的本地模型支持，包括 torch 等大型依赖
# 本地版本可以作为备选方案支持云端版本

fastapi
uvicorn
pydantic
pydantic-settings
python-multipart
pytest
pytest-asyncio
httpx
requests

# 本地 ASR 模型依赖
funasr>=0.4.0

# Supabase 支持
supabase

# 深度学习框架 - 根据您的硬件选择对应版本
# GPU 版本 (CUDA 12.1):
# torch==2.1.0+cu121
# torchaudio==2.1.0+cu121
# 
# GPU 版本 (CUDA 11.8):
# torch==2.1.0+cu118
# torchaudio==2.1.0+cu118
#
# CPU 版本:
# torch==2.1.0
# torchaudio==2.1.0

# 请根据实际情况取消注释上面的一行，或运行:
# pip install torch torchaudio -f https://download.pytorch.org/whl/torch_stable.html

# 语言检测
langdetect
textblob

# 可选：云端 API 支持（如果想在本地版本中也支持云端作为备选）
dashscope
