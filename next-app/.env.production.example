# HearSight 本地模式环境变量（示例）

全栈默认零配置：所有服务地址都是本机回环，数据落在 `<repo>/app_datas/`。
只有在偏离默认布局时才需要覆盖，把本文件复制为 `.env.production` 并修改。

```bash
# 数据根目录（媒体文件 + SQLite + 临时目录）
# 默认：<repo>/app_datas
HEARSIGHT_DATA_DIR=/home/ke/hearsight-data

# ASR 微服务（FunASR 本地模式，ai-services 之外的独立进程）
# 默认：http://127.0.0.1:8003
ASR_BACKEND_URL=http://127.0.0.1:8003
# 长音频识别超时（毫秒），默认 1800000（30 分钟）
ASR_TIMEOUT_MS=1800000

# LLM（OpenAI 兼容）。默认指向本机 llama-server；接外部 API 时改这三项
# 默认：http://127.0.0.1:8080/v1 ｜ local-no-key ｜ qwen
LLM_BASE_URL=http://127.0.0.1:8080/v1
LLM_API_KEY=local-no-key
LLM_MODEL=qwen
# 例：硅基流动
# LLM_BASE_URL=https://api.siliconflow.cn/v1
# LLM_API_KEY=sk-xxx
# LLM_MODEL=deepseek-ai/DeepSeek-V3.2-Exp

# Embedding（本地 bge-m3 微服务）。默认：http://127.0.0.1:8004/v1 ｜ bge-m3
EMBEDDING_BASE_URL=http://127.0.0.1:8004/v1
EMBEDDING_API_KEY=local-no-key
EMBEDDING_MODEL=bge-m3

# 外部工具路径（有默认探测顺序：环境变量 > ~/ffmpeg-env > PATH）
FFMPEG_PATH=/home/ke/ffmpeg-env/bin/ffmpeg
FFPROBE_PATH=/home/ke/ffmpeg-env/bin/ffprobe
YTDLP_PATH=/home/ke/hearsight-env/bin/yt-dlp
```
