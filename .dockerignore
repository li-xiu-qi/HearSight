# Docker build context ignore file
# Exclude large runtime data, caches and sensitive files from build context.

# Runtime data / large media / model caches
app_datas/
app_datas/download_videos/
app_datas/*.mp4
app_datas/*.m4a
app_datas/*.temp.mp4
model_cache/
torch_cache/
transformers_cache/

# Databases and cache files
*.sqlite3
backend/cache/cache.db

# Environment / secrets
.env
.env.*

# Node / Python dependencies and build artifacts
node_modules/
frontend/node_modules/
dist/
build/
frontend/dist/

# Virtualenvs and python cache
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo

# Logs, OS and git metadata
*.log
.DS_Store
Thumbs.db
.git
.gitignore
.github

# Test outputs and temporary results
results/
backend/tests/results/
tests/

# Note: keep lockfiles (package-lock.json / requirements.txt) and Dockerfile in context
# so builds are reproducible. Do NOT add them to this file.